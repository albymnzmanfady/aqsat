version: "3.8"

services:
  app:
    build: .
    ports:
      - "80:3001"
    volumes:
      - app-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  app-data: