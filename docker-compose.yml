services:
  app:
    build: .
    ports:
      - "10011:3001"
    volumes:
      - app-data:/app/data
    restart: unless-stopped
    environment:
      - NODE_ENV=production
      - PORT=3001

volumes:
  app-data: